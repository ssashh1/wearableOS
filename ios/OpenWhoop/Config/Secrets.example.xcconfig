// Copy to Secrets.xcconfig and fill in. Secrets.xcconfig is gitignored.
// The https:/$()/ trick prevents xcconfig from treating // as a comment.
WHOOP_BASE_URL = https:/$()/whoop.example.com
WHOOP_API_KEY = replace-me
WHOOP_DEVICE_ID = my-whoop
